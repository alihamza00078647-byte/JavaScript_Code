const obj1 = {
    name : "ali",
    age : "80"
}

const obj2 ={
    email : "qwerty@1234",
    contact : 987876543
}

let merge = Object.assign(obj1, obj2);
// Merge Objects by using assign operator

console.log(merge);

