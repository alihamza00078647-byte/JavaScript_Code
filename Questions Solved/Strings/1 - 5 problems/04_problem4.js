const str = "Hello Mina San";

console.log(`Character at Given is = ${str.charAt(8)}`);
// Use indexOf to find the Character at that index

console.log(`Index of Given Character is = ${str.lastIndexOf("M")}`);
// Will return index of that particular Character
