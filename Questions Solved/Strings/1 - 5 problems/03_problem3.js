let info = "Mr Red is like Blue";

// info.toUpperCase();
console.log(`String in Upper Case = ${info.toUpperCase()}`);

// info.toLowerCase();
console.log(`String in Lower Case = ${info.toLowerCase()}`);