let pad = 'HE';
console.log(pad);

// Padd Words at start Care at index 4 or like that 
let padding = pad.padStart(4, 'FG');
console.log(padding);

// Padd Words at the End Same like above

let paddingEnd = pad.padEnd(6, 'FG7');
console.log(paddingEnd);